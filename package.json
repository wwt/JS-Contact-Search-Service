{
  "name": "project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "pretest": "npm install && (npm run test || true)",
    "test": "./node_modules/.bin/mocha test --require @babel/register",
    "test:watch": "./node_modules/.bin/mocha test --require @babel/register --watch"
  },
  "dependencies": {
    "@babel/core": "^7.4.5",
    "@babel/plugin-transform-runtime": "^7.4.4",
    "@babel/preset-env": "^7.4.5",
    "@babel/register": "^7.4.4",
    "@babel/runtime": "^7.4.5",
    "chai": "^4.1.1",
    "mocha": "^6.1.4",
    "mocha-junit-reporter": "^1.13.0",
    "mocha-multi-reporters": "^1.1.4"
  }
}
